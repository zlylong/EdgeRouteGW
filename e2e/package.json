{
  "name": "proxygw-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:buttons": "playwright test tests/app-buttons.spec.js --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}

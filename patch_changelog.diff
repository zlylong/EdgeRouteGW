--- /root/proxygw/docs/CHANGELOG.md
+++ /root/proxygw/docs/CHANGELOG.md
@@ -1,5 +1,11 @@
 ## [Unreleased]
 
+## [1.7.15] - 2026-05-04
+### 🐛 修复 (Bug Fixes)
+- **防止 GFW DNS 投毒**: 将后端 OSPF 展开引擎的所有 geosite/domain DNS 解析请求统一路由至本地 Mosdns 实例 (127.0.0.1:53)，利用其分流机制和内置的 Xray SOCKS5 代理通道安全解析海外被墙域名，彻底斩断 OSPF 规则获取到如 119.29.0.0/16 这种脏 IP 的途径。
+- **缓存防污染遗传**: 在系统的自动更新脚本 (`scripts/update.sh`) 与手动缓存清理工具中，增加针对 `domain_resolve_cache`, `routes_table`, `geosite_expand_cache` 的全维度数据清洗步骤。确保遇到严重污染场景时大版本更新后不会再继承上一代的“脏”路由。
+
 ## [1.7.14] - 2026-05-04
 ### 🐛 修复 (Bug Fixes)
 - **DNS 解析尾巴修复**: 修复在设置内网 DNS 地址(如带路径或 scheme)时，错误地为其追加 socks5 代理尾巴的问题。
